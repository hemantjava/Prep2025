**🔹 Space Complexity:**
Recursion uses the call stack.
At the deepest point, before unwinding, there are n recursive calls on the stack.
Each call takes constant space, so stack space = O(n).
No extra data structures are used.
👉 Space Complexity = O(n)